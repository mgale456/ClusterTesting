---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: hypermind
  namespace: hypermind
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 15.29.54
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 15m
  timeout: 20m
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: ghcr.io/lklynet/hypermind
      pullPolicy: IfNotPresent
      tag: 0.12.2@sha256:fb4ebb341c19c984a42831c360bf10fefad2c969e193322f4f0f3b69a689ecd9
    securityContext:
      container:
        readOnlyRootFilesystem: false
    service:
      main:
        ports:
          main:
            port: &port 3000
            protocol: http
    workload:
      main:
        podSpec:
          containers:
            main:
              env:
                PORT: *port
                ENABLE_CHAT: true
                ENABLE_MAP: true
                VISUAL_LIMIT: 1000
    resources:
      limits:
        cpu: 1000m
        memory: 512Mi
      requests:
        cpu: 500m
        memory: 200Mi
    ingress:
      main:
        enabled: true
        ingressClassName: internal
        annotations:
          gethomepage.dev/instance: "internal"
        hosts:
          - host: hypermind.${INGRESS_URL}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - hypermind.${INGRESS_URL}
