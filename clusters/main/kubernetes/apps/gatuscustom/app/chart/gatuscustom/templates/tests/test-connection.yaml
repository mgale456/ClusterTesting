apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gatuscustom.fullname" . }}-test-connection"
  labels:
    {{- include "gatuscustom.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gatuscustom.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
