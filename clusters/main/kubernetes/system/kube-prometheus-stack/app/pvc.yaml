apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "prom"
  namespace: kube-prometheus-stack
  labels:
    app.kubernetes.io/name: prom-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 75Gi
  storageClassName: miroir-local
